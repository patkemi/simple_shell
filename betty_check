#include <stdio.>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    int count;

    for (count = 0; count < 10; count++)
    {
        printf("Hello, world!\n");
    }

    return 0;
}
